# OpenAI API key — if set, uses OpenAI embeddings (fast, no local GPU needed)
# Falls back to local model (Xenova/bge-m3) when not set or API unreachable
OPENAI_API_KEY=sk-proj-xxx

# Target directory to index/search (default: current directory)
# INDEX_DIR=/path/to/your/project/src

# Qdrant server URL (default: http://localhost:6333)
# QDRANT_URL=http://localhost:6333

# Qdrant collection name (default: codebase)
# QDRANT_COLLECTION=codebase

# Local embedding model (used when OpenAI is unavailable)
# EMBEDDING_MODEL=Xenova/bge-m3
